"use strict";
/**
 * Response attribute extraction for AWS Bedrock instrumentation
 *
 * Handles extraction of semantic convention attributes from InvokeModel responses including:
 * - Output message processing
 * - Tool call extraction
 * - Usage/token count processing
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractInvokeModelResponseAttributes = void 0;
const api_1 = require("@opentelemetry/api");
const openinference_core_1 = require("@arizeai/openinference-core");
const openinference_semantic_conventions_1 = require("@arizeai/openinference-semantic-conventions");
const bedrock_types_1 = require("../types/bedrock-types");
const attribute_helpers_1 = require("./attribute-helpers");
const invoke_model_helpers_1 = require("./invoke-model-helpers");
/**
 * Processes message content and extracts both text content and tool call attributes
 * Handles multi-modal responses by iterating through content blocks and processing
 * text content and tool use blocks with proper indexing
 *
 * @param params Object containing extraction parameters
 * @param params.message The normalized BedrockMessage containing structured content
 * @param params.span The OpenTelemetry span to set attributes on
 */
function addOutputMessageContentAttributes({ message, span, }) {
    if (!Array.isArray(message.content)) {
        return;
    }
    // Set the message role
    (0, attribute_helpers_1.setSpanAttribute)(span, `${openinference_semantic_conventions_1.SemanticConventions.LLM_OUTPUT_MESSAGES}.0.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_ROLE}`, message.role);
    // Use separate counters for different content types
    let contentBlockIndex = 0;
    let toolCallIndex = 0;
    // Process each content block in the message
    message.content.forEach((content) => {
        if ((0, bedrock_types_1.isTextContent)(content)) {
            // Handle text content with sequential content indexing
            const contentPrefix = `${openinference_semantic_conventions_1.SemanticConventions.LLM_OUTPUT_MESSAGES}.0.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_CONTENTS}.${contentBlockIndex}`;
            (0, attribute_helpers_1.setSpanAttribute)(span, `${contentPrefix}.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_CONTENT_TYPE}`, "text");
            (0, attribute_helpers_1.setSpanAttribute)(span, `${contentPrefix}.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_CONTENT_TEXT}`, content.text);
            contentBlockIndex++;
        }
        else if ((0, bedrock_types_1.isToolUseContent)(content)) {
            // Handle tool use content with sequential tool call indexing
            const toolCallPrefix = `${openinference_semantic_conventions_1.SemanticConventions.LLM_OUTPUT_MESSAGES}.0.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_TOOL_CALLS}.${toolCallIndex}`;
            (0, attribute_helpers_1.setSpanAttribute)(span, `${toolCallPrefix}.${openinference_semantic_conventions_1.SemanticConventions.TOOL_CALL_ID}`, content.id);
            (0, attribute_helpers_1.setSpanAttribute)(span, `${toolCallPrefix}.${openinference_semantic_conventions_1.SemanticConventions.TOOL_CALL_FUNCTION_NAME}`, content.name);
            (0, attribute_helpers_1.setSpanAttribute)(span, `${toolCallPrefix}.${openinference_semantic_conventions_1.SemanticConventions.TOOL_CALL_FUNCTION_ARGUMENTS_JSON}`, JSON.stringify(content.input));
            toolCallIndex++;
        }
    });
}
/**
 * Extracts output messages attributes from InvokeModel response body
 * Processes response content and sets OpenInference output message attributes
 * Handles both simple text responses and complex multi-modal responses
 *
 * @param params Object containing extraction parameters
 * @param params.responseBody The parsed response body containing content and metadata
 * @param params.span The OpenTelemetry span to set attributes on
 */
const extractOutputMessagesAttributes = (0, openinference_core_1.withSafety)({
    fn: ({ normalizedMessage, span }) => {
        (0, attribute_helpers_1.setSpanAttribute)(span, `${openinference_semantic_conventions_1.SemanticConventions.LLM_OUTPUT_MESSAGES}.0.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_ROLE}`, normalizedMessage.role);
        // Determine if this is a simple text response or complex multimodal response
        if ((0, invoke_model_helpers_1.isSimpleTextResponse)(normalizedMessage)) {
            // For simple text responses, use the message content
            // No cast needed - type guard ensures content[0] is TextContent
            const textBlock = normalizedMessage.content[0];
            (0, attribute_helpers_1.setSpanAttribute)(span, `${openinference_semantic_conventions_1.SemanticConventions.LLM_OUTPUT_MESSAGES}.0.${openinference_semantic_conventions_1.SemanticConventions.MESSAGE_CONTENT}`, textBlock.text);
        }
        else {
            // For complex multimodal responses, use the detailed message structure
            addOutputMessageContentAttributes({ message: normalizedMessage, span });
        }
    },
    onError: (error) => {
        api_1.diag.warn("Error extracting output messages attributes:", error);
    },
});
/**
 * Extracts token usage attributes from InvokeModel response body
 * Uses normalized usage attributes to handle different provider formats
 * Processes standard and cache-related token counts following OpenInference conventions
 *
 * @param params Object containing extraction parameters
 * @param params.responseBody The parsed response body containing usage statistics
 * @param params.modelType The LLM system type to determine extraction strategy
 * @param params.span The OpenTelemetry span to set attributes on
 */
function extractUsageAttributes({ responseBody, modelType, span, }) {
    const usage = (0, invoke_model_helpers_1.normalizeUsageAttributes)(responseBody, modelType);
    if (!usage) {
        return;
    }
    // Token counts - only set if defined
    if (usage.input_tokens !== undefined) {
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.LLM_TOKEN_COUNT_PROMPT, usage.input_tokens);
    }
    if (usage.output_tokens !== undefined) {
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.LLM_TOKEN_COUNT_COMPLETION, usage.output_tokens);
    }
    if (usage.total_tokens !== undefined) {
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.LLM_TOKEN_COUNT_TOTAL, usage.total_tokens);
    }
    if (usage.cache_read_input_tokens !== undefined) {
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.LLM_TOKEN_COUNT_PROMPT_DETAILS_CACHE_READ, usage.cache_read_input_tokens);
    }
    if (usage.cache_creation_input_tokens !== undefined) {
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.LLM_TOKEN_COUNT_PROMPT_DETAILS_CACHE_WRITE, usage.cache_creation_input_tokens);
    }
}
/**
 * Extracts semantic convention attributes from InvokeModel response and sets them on the span
 * Main entry point for processing InvokeModel API responses with comprehensive error handling
 *
 * Processes:
 * - Response body parsing from multiple formats
 * - Output messages with content structure
 * - Tool call attributes from response blocks
 * - Token usage statistics including cache metrics
 *
 * @param params Object containing extraction parameters
 * @param params.span The OpenTelemetry span to set attributes on
 * @param params.response The InvokeModelResponse to extract attributes from
 */
exports.extractInvokeModelResponseAttributes = (0, openinference_core_1.withSafety)({
    fn: ({ span, response, modelType, }) => {
        if (!response.body) {
            return;
        }
        const responseBody = (0, invoke_model_helpers_1.parseResponseBody)(response);
        if (!responseBody) {
            return;
        }
        //extract full response body as primary output
        const outputValue = JSON.stringify(responseBody);
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.OUTPUT_VALUE, outputValue);
        (0, attribute_helpers_1.setSpanAttribute)(span, openinference_semantic_conventions_1.SemanticConventions.OUTPUT_MIME_TYPE, "application/json");
        // Normalize the response body to a standard BedrockMessage format
        const normalizedMessage = (0, invoke_model_helpers_1.normalizeResponseContentBlocks)(responseBody, modelType);
        if (normalizedMessage) {
            // Extract all response attributes using named parameters
            extractOutputMessagesAttributes({ normalizedMessage, span });
        }
        extractUsageAttributes({ responseBody, modelType, span });
    },
    onError: (error) => {
        api_1.diag.warn("Error extracting InvokeModel response attributes:", error);
    },
});
//# sourceMappingURL=invoke-model-response-attributes.js.map