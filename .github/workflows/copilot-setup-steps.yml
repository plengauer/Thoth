on:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml
jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    steps:
      - run: |
          for script_file in /home/runner/work/_temp/*-action-main/script/*.sh; do            
            echo "$script_file >>>>>"
            cat "$script_file"
            echo "<<<<<"
          done
      - run: |
          :
    permissions:
      actions: read
