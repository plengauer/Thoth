name: Autoversion
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 1 * *'
  pull_request:
    types: [ closed ]
jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: none
      actions: read
    steps:
      - uses: plengauer/opentelemetry-github/actions/instrument/job@v5.30.1
        with:
          secrets_to_redact: '["${{ github.token }}","${{ secrets.ACTIONS_GITHUB_TOKEN }}","${{ secrets.OPENAI_TOKEN }}"]'
        env:
          OTEL_EXPORTER_OTLP_ENDPOINT: '${{ secrets.OTEL_EXPORTER_OTLP_ENDPOINT }}'
          OTEL_EXPORTER_OTLP_HEADERS: '${{ secrets.OTEL_EXPORTER_OTLP_HEADERS }}'
      - id: should-autoversion
        run: |
          case "$GITHUB_EVENT_NAME" in
            pull_request) cat "$GITHUB_EVENT_PATH" | jq .pull_request.number -r | xargs -I '{}' curl ${GITHUB_API_URL:-https://api.github.com}/repos/"$GITHUB_REPOSITORY"/issues/'{}'/comments'?per_page=100' | jq '.[] | select(.body == "!release") | .author_association' -r | grep -q OWNER && echo true || echo false;;
            *) echo true;;
          esac | xargs -I '{}' echo result='{}' >> "$GITHUB_OUTPUT"
      - uses: plengauer/autoversion@v1.4.0
        if: ${{ steps.should-autoversion.outputs.result == 'true' }}
        with:
          github_token: ${{ secrets.ACTIONS_GITHUB_TOKEN }}
          openai_token: ${{ secrets.OPENAI_TOKEN }}
          path_include: ./src:./actions
          increment_threshold: minor
